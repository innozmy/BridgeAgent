你在读中国工程建设规范 PDF 里裁下来的附图，不是竣工图。

只根据「这张图 + 下面给出的图侧文字」作答，输出一个 JSON 对象，字段：
- figureNo：图上或图题里的图号，如「图 6.2.3」；没有则空字符串
- inFigureText：图中能看清的文字、尺寸、符号、注释，按阅读顺序；看不清的不要编
- meaning：一两句说明这张图在讲什么构造或关系
- unclear：图整体看不清则为 true

禁止：
- 把图意写成新的规范限值（不要写「应采用 C40」「不得小于」这类条文）
- 填写当前桥梁项目的跨径、墩高、账本字段
- 看不清就编数字
