---
description: 调试结束后释放 8080 / 8001，方便使用者自己启动 Spring 与 Python
alwaysApply: true
---

# 调试后释放端口

本机调试约定：Spring 用 **8080**，Python Agent 用 **8001**。这两端口留给使用者自己启动。

- **不要**在对话结束时把 Spring / Python 留在后台占着 8080、8001。
- 本回合若启动过 `spring-boot:run`、`python app.py`，或发现 8080 / 8001 仍被本回合（或先前助手）拉起的进程占用：**任务收尾时结束占用进程**，确认端口空闲。
- 不要顺手停 Vite **5173**，除非使用者明确要求。
- 使用者说「帮我停掉 / 取消占用」时，立刻释放对应端口。
