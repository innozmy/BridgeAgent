---
description: 活文档：锁定架构、已实现、下一步、暂缓。每次拍板或交付后必须改这一份。
alwaysApply: true
---

# 规划文档同步

`docs/roadmap.md` 是项目的单一规划底稿（架构确认、已实现功能、下一步、暂缓）。

在本仓库做任务时：

- 用户新确认一条架构/规则，或否决/改口：当天改「已锁定」或「暂缓」，不要只留在聊天里。
- 新功能合并进代码：把条目从「下一步」挪到「已实现」，写清接口或页面。
- 新规划（含延后项如记忆、RAG）：写入对应章节，并标明尚未实现。
- 不要另起第二份互相打架的路线图；细节仍可在聊天里讨论，拍板后以该文件为准。

记忆由 Spring 落 MySQL，Python 只跑 Agent。问询聊天不是工程记忆。规划见该文件「Agent 记忆」节。
